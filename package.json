{
  "name": "mrc-one-pager",
  "version": "1.2.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/assets && cp index.html warranty-db.js dist/ && cp assets/*.js dist/assets/"
  }
}
